#include <bits/stdc++.h>
using namespace std;
int main()
{
    string st;
    getline(cin,st);
    reverse(st.begin(), st.end());
    for (auto  i = st.begin() ; i !=  st.end(); i++)
    {
        cout << st << endl;
    }
    
    

    return 0;
}